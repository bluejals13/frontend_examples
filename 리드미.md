이해했습니다. 제공해주신 PHP + jQuery 기반 도서 관리 시스템의 코드를 바탕으로 깔끔하고 가벼운 `README.md` 형식의 오버뷰를 작성해 드리겠습니다.

---

# 📚 도서 관리 시스템 (PHP + jQuery 버전)

이 프로젝트는 최신 프론트엔드 프레임워크(React, Vue 등) 없이, 전통적이고 실용적인 **PHP와 jQuery** 조합을 사용하여 구현한 도서 관리 시스템(CRUD)입니다. 데이터베이스 대신 PHP 세션(`$_SESSION`)을 활용하여 가볍게 동작 원리를 테스트할 수 있도록 설계되었습니다.

## 🗂️ 프로젝트 구조

```md
book_php_jquery/
├── docker-compose.yml    # PHP 8.3 Apache 웹 서버 구동을 위한 Docker 설정
└── html/                 # 웹 루트 디렉터리
    ├── index.php         # 메인 View (UI 뼈대 및 입력 폼)
    ├── style.css         # 스타일 시트
    ├── app.js            # jQuery 기반 프론트엔드 제어 및 DOM 조작 로직
    └── api/
        └── books.php     # 도서 데이터 CRUD 처리를 담당하는 PHP REST API

```

## 🚀 실행 방법

이 프로젝트는 Docker를 사용하여 격리된 환경에서 실행됩니다. 로컬 환경에 PHP나 Apache를 직접 설치할 필요가 없습니다.

1. **프로젝트 폴더로 이동:** 터미널에서 `book_php_jquery/` 디렉터리로 이동합니다.
2. **Docker 컨테이너 실행:**
```bash
docker-compose up -d

```


3. **브라우저 접속:** `http://localhost:8080` 으로 접속하여 시스템을 확인합니다.
4. **종료 시:**
```bash
docker-compose down

```



## ⚙️ 기술 스택 및 특징

* **Backend:** PHP 8.3 (Apache)
* API 서버 역할 수행 (`/api/books.php`)
* DB 없이 세션을 이용한 메모리 데이터 관리


* **Frontend:** HTML5, CSS3, jQuery 3.7.1 (CDN)
* AJAX 기반 비동기 데이터 통신
* 이벤트 위임(Event Delegation)을 통한 동적 DOM 제어


* **Infrastructure:** Docker & Docker Compose

## 🆚 렌더링 패러다임 비교 (PHP+jQuery vs React)

이 프로젝트는 "명령형 DOM 조작" 방식을 사용합니다. 상태를 기반으로 화면을 자동으로 갱신하는 React(선언형) 방식과의 핵심 차이는 다음과 같습니다.

| 구분 | PHP + jQuery (본 프로젝트) | React (비교 대상) |
| --- | --- | --- |
| **렌더링 주체** | 브라우저 (jQuery 명령에 의한 DOM 직접 교체) | 브라우저 (Virtual DOM 비교 연산 후 최소 패치) |
| **데이터 동기화** | 이벤트(Click, Submit) 발생 시 DOM에서 데이터 추출 (`serialize()`) | 입력 즉시 메모리 상태(State)와 양방향/단방향 동기화 |
| **목록 갱신 방식** | 서버 통신 후 조립된 전체 HTML 문자열로 `<tbody>`를 통째로 덮어씀 (`.html()`) | 변경된 데이터(State)를 감지하여 해당하는 특정 노드만 업데이트 |
| **이벤트 처리** | 동적 생성 요소 보호를 위해 부모 노드에 이벤트 위임 필요 (`.on('click', ... )`) | 컴포넌트 렌더링 시점에 개별 노드에 직접 이벤트 바인딩 (`onClick`) |

이 비교를 통해 두 기술이 해결하고자 하는 문제는 같으나, 데이터를 화면에 반영하는 접근 방식(명령형 vs 선언형)이 어떻게 다른지 이해할 수 있습니다.